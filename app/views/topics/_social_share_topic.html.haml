.share-topic-link
  Ask Friends - should you buy it?

  %ul.horizontal-list.mtop20
    %li
      = link_to topic_share_path(topic) do
        .email-icon

    %li
      - if current_user.hasTwitter?
        = link_to social_ask_twitter_path(topic.shortcode), remote: true do
          .twitter-icon{id: topic.tweeted_by?(current_user) ? "tweeted" : ""}
      - else
        = link_to user_omniauth_authorize_path(:twitter) do
          .twitter-icon

    %li
      - if current_user.hasFacebook?
        = link_to social_ask_facebook_path(topic.shortcode), remote: true do
          .facebook-icon{id: topic.recommended_by?(current_user) ? "recommended" : ""}
      - else
        = link_to user_omniauth_authorize_path(:facebook) do
          .facebook-icon
  .clear
