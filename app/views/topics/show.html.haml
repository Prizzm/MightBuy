.show-topic
  .row.topic-links.ptop30.pbot
    - if current_user
      .span2
        = link_to profile_path do
          #home-link
      .span4.offset4
        %ul
          %li
            = link_to edit_topic_path(@topic.shortcode) do
              #edit-topic
          %li
            = link_to topic_path(@topic.shortcode), method: :delete do
              #delete-topic
