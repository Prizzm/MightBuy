$(".flash-notice").remove();
$("body").prepend("<%= escape_javascript(render partial: 'shares/flash_notice') -%>");
$("#topic-recommend").html("<%= escape_javascript(render partial: 'topics/topic_recommend', locals: { topic: @topic }) -%>");
