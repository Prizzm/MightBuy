<%- if current_user -%>
var response = $(<%= e(render("responses/recommendation/response", :response => @topic_response)) %>);

givepoints(<%= recommendation_points_earned(@topic_response) %>);
//$('.group.respond').after(<%= e(render('topics/register')) %>);

$('.group.respond').slideUp(300, function () {
  $(this).empty();
  $(this).append(response);
  $(this).slideDown(300, function () {
    fbparse();
    register();
    pointstips();
    pointsdisabler();
    
  });
});

<% else -%>
  window.location = "<%= new_user_session_path -%>";
<% end -%>
