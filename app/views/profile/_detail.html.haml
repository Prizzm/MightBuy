= link_to new_topic_path, class: "btn btn-red add-item" do
  %i.icon-plus-sign.icon-white
  Add an item

%ol.list.topics#pageless-topic-listing{"data-total-pages" => "#{@user.topics.mightbuy.latest_posts.num_pages}", "data-pageless-url" => "/me"}
  = placeholder latest_posts do |collection|
    = render partial: "topics/topic", collection: collection
