.user-signin
  .row
    .span7.bevel-line-vertical-right
      %p
        Sign in to MightBuy!

      = simple_form_for resource, url: new_user_session_path, html: { class: 'form-horizontal' } do |f|
        = f.input :email
        = f.input :password
        %p.forgot-password
          = link_to("Forgot your password?",new_user_password_path)
        .pull-right.mright75.mbot30
          = f.submit "Sign in!", id: 'sign-in-submit-button', :class=>"btn btn-red btn-rounded small-case"


    .span5.social-login
      %p
        Sign in with Facebook & Twitter!

      #social-buttons
        = link_to user_omniauth_authorize_path(:facebook), id: "facebook-signin" do
          = image_tag("facebook-signin.jpg");
        = link_to user_omniauth_authorize_path(:twitter), id: "twitter-signin" do
          = image_tag("twitter-signin.jpg");

      .ptop19
        %p
          Don't have an account yet?
      .pull-right.mtop15.mright10
        = link_to(new_user_registration_path, :class=>"btn btn-red btn-rounded small-case") do
          Try it now!